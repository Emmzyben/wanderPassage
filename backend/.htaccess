Options -Indexes

# Allow cross-origin requests (adjust origin in production)
# CORS headers are handled by backend/utils/cors.php

# Preflight OPTIONS requests are handled by backend/utils/cors.php (via db.php)
